{
  "name": "fabiobooks-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
  "dev": "npm-run-all --parallel dev:*",
  "dev:web": "npm --workspace apps/web run dev",
  "dev:api": "npm --workspace apps/api run dev"
  } 
}
